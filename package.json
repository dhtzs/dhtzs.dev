{
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0-beta.8",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^4.0.0-beta.8"
  },
  "scripts": {
    "update-deps": "npm install @tailwindcss/postcss@next postcss postcss-cli tailwindcss@next --save-dev",
    "build": "rm -rf public && hugo",
    "server": "rm -rf public && hugo server"
  }
}
